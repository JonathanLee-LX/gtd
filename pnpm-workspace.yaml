# Single-package workspace so pnpm 7–9 `store path` (used by
# actions/setup-node cache: pnpm) does not fail with
# "packages field missing or empty".
packages:
  - "."

# Allow the native build scripts required by the Worker toolchain.
onlyBuiltDependencies:
  - better-sqlite3
  - esbuild
  - workerd
